let table;
let yisang,faust,donquixote,ryoshu,meursault,honglu,heathcliff,ishmael,rodion,sinclair,gregor;

function setup() {
  createCanvas(windowWidth, windowHeight);
  table = loadTable('Data/Sinners.csv',',','header');
}

function draw() {
  background(220);
}